# Workspace memory

## 2026-08-31 — product scoped

Flatten done. Signup is email + password. Confirm links use PKCE
`?code=` on `/auth/confirm`. Poppins + Outfit fonts. Check-in
UI still waits on a live schema.
